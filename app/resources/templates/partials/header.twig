<header>
	<a id="logo" href="{{ url('/') }}">
		<img src="{{ url('img/logo.png') }}" alt="logo Astride for header">
	</a>
	<button data-active="false" id="hamburger">
		<span></span>
		<span></span>
		<span></span>
	</button>
	{% if athlete %}
		<ul class="menu-options">
			<li>
				<span class="material-symbols-outlined">home</span>
				<a href="{{ url('/') }}">Dashboard</a>
				<hr>
			</li>
			<li>
				<span class="material-symbols-outlined">person</span>
				<a href="{{ url('/trainers') }}">Search trainers</a>
				<hr>
			</li>
			<li>
				<span class="material-symbols-outlined">search</span>
				<a href="{{ url('/my-profile') }}">My profile</a>
				<hr>
			</li>
			<li class="logout-header">
				<a href="{{ url('/logout') }}">Logout</a>
			</li>
		</ul>
	{% else %}
		<ul class="menu-options">
			<li>
				<span class="material-symbols-outlined">home</span>
				<a href="{{ url('/') }}">Dashboard</a>
				<hr>
			</li>
			<li>
				<span class="material-symbols-outlined">groups</span>
				<a href="{{ url('/my-athletes') }}">My athletes</a>
				<hr>
			</li>
			<li>
				<span class="material-symbols-outlined">search</span>
				<a href="{{ url('/my-profile') }}">My profile</a>
				<hr>
			</li>
			<li class="logout-header">
				<a href="{{ url('/logout') }}">Logout</a>
			</li>
		</ul>
	{% endif %}
</header>
